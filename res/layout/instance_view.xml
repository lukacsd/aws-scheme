<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <LinearLayout
        android:id="@+id/instanceViewLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="@dimen/resourceStateViewMarginBottom"
        android:layout_toLeftOf="@+id/instanceStateText"
        android:layout_toStartOf="@+id/instanceStateText"
        android:background="@drawable/border"
        android:orientation="vertical" />

    <TextView
        android:id="@+id/instanceStateText"
        android:layout_width="@dimen/resourceStateViewWidth"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_marginBottom="@dimen/resourceStateViewMarginBottom" />

</RelativeLayout>