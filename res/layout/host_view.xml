<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <LinearLayout
        android:id="@+id/hostViewLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="@dimen/resourceStateViewMarginBottom"
        android:layout_toLeftOf="@+id/hostnameStateText"
        android:layout_toStartOf="@+id/hostnameStateText"
        android:background="@drawable/host_border"
        android:orientation="vertical" />

    <TextView
        android:id="@+id/hostnameStateText"
        android:layout_width="@dimen/resourceStateViewWidth"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_marginBottom="@dimen/resourceStateViewMarginBottom" />

</RelativeLayout>